export const UPDATE_REPO_OWNER = 'solywsh'
export const UPDATE_REPO_NAME = 'Renderer-Update-Center'

// Release asset names
export const UPDATE_ASSET_FILENAME = 'renderer-dist-encrypted.bin'
export const UPDATE_SHA256_FILENAME = 'renderer-dist-encrypted.sha256'

// Toggle whether to require SHA256 before download. If false, we rely on
// package signature verification after download.
export const REQUIRE_SHA256_FOR_DOWNLOAD = false
